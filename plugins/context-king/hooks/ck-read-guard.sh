#!/usr/bin/env bash
# ck-read-guard: PreToolUse hook for the built-in Read tool.
#
# Native Read is allowed. Exploration discipline is enforced by protocol docs
# and search guards (find-files first, find-files fallback), not by blocking Read.

exit 0
